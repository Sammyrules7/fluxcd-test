apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis
  namespace: default
spec:
  interval: 1m0s
  chart:
    spec:
      chart: redis
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: default
  values:
    replicaCount: 1
    auth:
      enabled: false
    master:
      persistence:
        enabled: false
